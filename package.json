{
  "name": "pipegpu.graph",
  "version": "0.0.3",
  "description": "webgpu based frame graph",
  "main": "index.ts",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@webgpu/types": "^0.1.60",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "kiwi.matrix": "^0.6.4",
    "pipegpu": "^0.0.9"
  }
}